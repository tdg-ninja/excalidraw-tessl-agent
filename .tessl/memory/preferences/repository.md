# Repository Notes

- Excalidraw monorepo using Yarn 1 workspaces.
- Core editor code lives under `packages/excalidraw/`.
- App code lives under `excalidraw-app/`.
- Useful checks: `yarn test:typecheck`, focused `yarn test -- <path> --watch=false`, and `yarn fix`.
