# Plugin Preferences

- Repository-local plugin directory: `plugins/`.
- Local issue implementation skill: `plugins/issue-implementer/skills/implement-issue/SKILL.md`.
- Register local plugins in `tessl.json` with `file:` sources while developing.
- Keep SKILL.md files concise and workflow-focused.
