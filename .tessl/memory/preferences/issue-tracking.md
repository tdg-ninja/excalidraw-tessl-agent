# Issue Tracking Preferences

- Issue tracker: GitHub Issues.
- The autonomous harness accepts a GitHub issue number as its primary input.
- Issues may be read from `upstream` when needed, but implementation PRs target `origin` unless a human explicitly overrides this.
- Implementation PRs should include `Closes #<issue-number>` in the body.
